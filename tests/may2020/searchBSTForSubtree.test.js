import searchBST from '../../problems/may2020/searchBSTForSubtree';

describe('Search BST for Subtree Tests', () => {

  test('Returns null when no root input', () => {
    expect(searchBST(null, 9)).toBe(null);
  });

  // write tests that conform to what the output shout be

});