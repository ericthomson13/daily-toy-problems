const twoSum = require('../problems/TwoSum');

describe('Two Sums Tests', () => {
  it('should return the correct indices', () => {
    expect(twoSum([1,2], 3)[0]).toBe(0);
  });
  xit('should return false when the inputs are invalid', () => {

  });
});
