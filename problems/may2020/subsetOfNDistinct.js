/*

Write a recursive function that given a list of n distinct elements list all subsets of those elements

*/

/**
 * @param {[]} elems
 * @return {[]}
*/


const subsetOfN = (elems) => {
  if (elems.length < 2) return elems;

  const result = [];

  // recursive call to build out all possible trees

  return result;
};

export default subsetOfN;
